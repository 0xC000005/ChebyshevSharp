# v0.12 Quality and Documentation Implementation Plan

> Required workflow: keep this file committed and update it on each branch. Do not rely on chat history as the only source of unresolved scope.

## Goal

Ship a v0.12.0 quality release that hardens tensor-size arithmetic, expands validation tooling, deepens tests, and reorganizes documentation without regressing the current test baseline.

## Baseline Invariants

- Planning branch: `main`.
- Baseline command: `dotnet test --verbosity minimal`.
- Baseline at planning time: `1125` passed, `0` failed.
- Required before every PR: `dotnet test --verbosity minimal`.
- Required before release PR: `dotnet build --configuration Release`, `dotnet test --configuration Release --verbosity minimal`, `dotnet pack src/ChebyshevSharp --configuration Release`, and `docfx docs/docfx.json`.
- Keep unrelated untracked files, including root `AGENTS.md`, out of these PRs unless explicitly requested.

## PR 1: Tensor Size Safety

**Branch:** `quality/v0.12-tensor-size-safety`

**Purpose:** Remove recurrence of the overflow class fixed in issue #23 across full-grid, serialization, axis-kernel, slider, and TT materialization paths.

### Task 1.1: Shared Shape Arithmetic

- [x] Add `src/ChebyshevSharp/Internal/TensorShape.cs`.
- [x] Implement `CheckedProduct`, `ProductAtMost`, `CheckedByteSize`, and `RequireArrayLength`.
- [x] Include positive-dimension validation and dimension-list diagnostics.
- [x] Add `tests/ChebyshevSharp.Tests/TensorShapeTests.cs`.
- [x] Verify with `dotnet test --filter "FullyQualifiedName~TensorShapeTests" --verbosity minimal`.

### Task 1.2: Apply Shape Arithmetic

- [x] Harden `ChebyshevApproximation` full-grid construction, value factories, evaluation-point counts, coordinate arrays, and internal stride/flatten helpers.
- [x] Harden `ChebyshevSpline` piece counts, fixed-grid build totals, auto-N placeholder handling, `FromValues`, `SetOriginalFunctionValues`, slicing/integration shape changes, and evaluation-point APIs.
- [x] Harden `ChebyshevSlider` total build counts, progress offsets, diagnostics, and evaluation-point materialization.
- [x] Harden `Internal/ExtrudeSlice`, `Internal/BarycentricKernel`, `Internal/PcbFormat`, and `Internal/Sensitivity`.
- [x] Extend TT safeguards in `ChebyshevTT`, `TensorTrainKernel`, `TensorTrainExtrude`, and `TensorTrainAlgebra` where rank/grid products feed allocations.
- [x] Add regression tests for large shapes that must throw before allocation or wrapped arithmetic.
- [x] Verify with `dotnet test --filter "FullyQualifiedName~Overflow|FullyQualifiedName~TensorShape|FullyQualifiedName~ExtrudeSlice|FullyQualifiedName~Spline|FullyQualifiedName~TensorTrain" --verbosity minimal`.
- [x] Verify full suite with `dotnet test --verbosity minimal`.

### PR 1 Evidence

- Focused shape and PCB regressions: `36/36` passed.
- Expanded overflow/spline/TT focused suite: `346/346` passed.
- Full suite: `1146/1146` passed.
- Remaining product-scan hits are guarded helper implementations, scalar numerical products, read-side `CheckedMul`, or manually guarded TT helpers.

## PR 2: Static Analysis, CI, and Package Validation

**Branch:** `quality/v0.12-ci-validation`

- [x] Add or update `.editorconfig` for indentation, final newline, naming conventions, and selected analyzer severities.
- [x] Add `Directory.Build.props` with analyzers, latest analysis level, code-style enforcement, and Debug overflow checking if compatible.
- [x] Add package validation to `src/ChebyshevSharp/ChebyshevSharp.csproj`.
- [x] Expand CI to run format verification, analyzer build, tests with coverage, package pack, and DocFX build.
- [x] Verify Codecov upload still runs on PRs.

### PR 2 Evidence

- `dotnet format --verify-no-changes --verbosity minimal`: passed.
- `dotnet build --verbosity minimal`: passed with `0` warnings and `0` errors.
- `dotnet test --verbosity minimal`: passed `1146/1146`.
- `dotnet build --configuration Release --verbosity minimal`: passed with `0` warnings and `0` errors.
- `dotnet pack src/ChebyshevSharp --configuration Release --no-build --output artifacts/packages --verbosity minimal`: created `ChebyshevSharp.0.11.1.nupkg`.
- `docfx docs/docfx.json`: passed with `0` warnings and `0` errors.
- GitHub Actions PR #26: `Format, Pack, and Docs`, `.NET 8`, `.NET 10`, and aggregate gates passed. Codecov upload ran and queued the coverage report; no separate `codecov/patch` status was emitted because this PR changed validation/configuration and formatter-only code.

## PR 3: Property Testing and Mutation Testing

**Branch:** `quality/v0.12-test-depth`

- [x] Add FsCheck/xUnit support and deterministic generators for small dimensions, node counts, domains, and evaluation points.
- [x] Add properties for batch-vs-loop evaluation, save/load preservation, extrude/slice recovery, algebra identities, and TT reorder preservation.
- [x] Add `stryker-config.json` targeting high-risk numerical files first.
- [x] Add a manual or scheduled mutation-testing workflow; do not make mutation score a hard gate until runtime and flake rate are known.
- [x] Document local property and mutation test commands.

### PR 3 Evidence

- Focused property suite: `dotnet test --filter "FullyQualifiedName~PropertyTests" --verbosity minimal` passed `5/5`.
- Full suite: `dotnet test --verbosity minimal` passed `1151/1151`.
- Format and build gates passed with `dotnet format --verify-no-changes --verbosity minimal`, `dotnet build --verbosity minimal`, and `dotnet build --configuration Release --verbosity minimal`.
- Package validation produced `ChebyshevSharp.0.11.1.nupkg` with `dotnet pack src/ChebyshevSharp --configuration Release --no-build --output artifacts/packages --verbosity minimal`.
- Docs build passed with `docfx docs/docfx.json`.
- Local Stryker smoke against `**/Internal/TensorShape.cs`: `54` mutants tested, `38` killed, `16` survived, baseline score `61.29%`.

## PR 4: Documentation Architecture and Examples

**Branch:** `docs/v0.12-diataxis-docs`

- [x] Update stale README and package-description claims, including PyChebyshev parity and ChebyshevTT availability.
- [x] Rework docs around Diataxis: tutorials, how-to guides, concepts, and reference.
- [x] Add `which-class.md`, `testing-and-validation.md`, and `citations.md`.
- [x] Add runnable `examples/QuickStart` and `examples/TensorTrainHighDim`.
- [x] Add TT documentation warning that full-grid materialization is intentionally guarded for high-dimensional tensors.
- [x] Verify with `docfx docs/docfx.json` and `dotnet test --verbosity minimal`.

### PR 4 Evidence

- `dotnet format --verify-no-changes --verbosity minimal`: passed.
- `dotnet build --verbosity minimal`: passed with `0` warnings and `0` errors.
- `dotnet test --verbosity minimal`: passed `1151/1151`.
- `dotnet run --project examples/QuickStart/QuickStart.csproj --no-build`: passed.
- `dotnet run --project examples/TensorTrainHighDim/TensorTrainHighDim.csproj --no-build`: passed.
- `dotnet build --configuration Release --verbosity minimal`: passed with `0` warnings and `0` errors.
- `dotnet pack src/ChebyshevSharp --configuration Release --no-build --output artifacts/packages --verbosity minimal`: created `ChebyshevSharp.0.11.1.nupkg`.
- `docfx docs/docfx.json`: passed with `0` warnings and `0` errors.

## PR 5: Release Preparation

**Branch:** `release/v0.12.0`

- [x] Set `<Version>0.12.0</Version>`.
- [x] Keep `<PyChebyshevParity>0.21.1</PyChebyshevParity>` unless the Python reference advances.
- [x] Set `<InformationalVersion>0.12.0+pychebyshev.0.21.1</InformationalVersion>`.
- [x] Add changelog section `## [0.12.0] - 2026-05-02 - Quality and documentation hardening`.
- [x] Verify mathematical citation links and book/source existence before release.
- [ ] Merge release PR, create GitHub release `v0.12.0`, and verify NuGet publish workflow.

### PR 5 Evidence

- `dotnet format --verify-no-changes --verbosity minimal`: passed.
- `dotnet build --configuration Release --no-restore --verbosity minimal`: passed with `0` warnings and `0` errors.
- `dotnet test --configuration Release --no-build --verbosity minimal --collect:"XPlat Code Coverage"`: passed `1154/1154`; wrote Cobertura coverage.
- `dotnet run --project examples/QuickStart/QuickStart.csproj --configuration Release --no-build`: passed.
- `dotnet run --project examples/TensorTrainHighDim/TensorTrainHighDim.csproj --configuration Release --no-build`: passed.
- `dotnet pack src/ChebyshevSharp --configuration Release --no-build --output artifacts/packages --verbosity minimal`: created `ChebyshevSharp.0.12.0.nupkg`.
- `docfx docs/docfx.json`: passed with `0` warnings and `0` errors.
- Authored Markdown link/source check: `76` links checked, `0` failures. Project-site links were checked against `docs/_site`; external links were checked over HTTP with GET fallback.
- DOI metadata check: Crossref resolved all `17` DOI-backed citations and exposed titles/authors matching the citation list. This pass caught and corrected an invalid ATAP DOI before release.
- Book/text existence checks: Trefethen's ATAP author page confirms first publication in `2013` and extended edition in `2019`; Crossref confirms the SIAM extended-edition DOI; Crossref confirms *Spectral Methods in MATLAB*; Wiley confirms Ruiz/Zeron, *Machine Learning for Risk Calculations*, ISBN `978-1-119-79138-6`; Oxford confirms Trefethen's 2017 Chebyshev-series lecture PDF; arXiv confirms Glau/Kressner/Statti `1902.04367`.

## Session Log

- 2026-05-02: Created v0.12 master plan after user requested durable tracking, PR sequencing, and no-regression implementation strategy.
- 2026-05-02: Completed PR 1 tensor-size safety implementation. Added shared `TensorShape`, hardened high-risk allocation/count paths, added regression coverage, and verified full suite with `1146/1146` passing.
- 2026-05-02: Completed PR 2 local implementation. Added format/analyzer configuration, package validation, CI validation job, and verified format, build, tests, pack, and docs locally.
- 2026-05-02: Completed PR 3 local implementation. Added deterministic FsCheck properties, manual/scheduled mutation testing, validation docs, and Stryker smoke evidence for tensor-shape mutation coverage.
- 2026-05-02: Completed PR 4 local implementation. Updated stale README/package claims, reorganized docs around tutorials/how-to/concepts/reference, added class-selection and citation pages, added runnable examples, and documented TT dense-grid guards.
- 2026-05-02: Completed PR 5 local release prep. Set version metadata to v0.12.0, added the v0.12.0 changelog, and made the TT JSON round-trip version assertion derive from assembly metadata.
- 2026-05-02: Added final source-verification pass for mathematical citations, fixed the ATAP citation to the correct SIAM extended-edition DOI, encoded DOI URLs with parentheses, added missing authoritative links, and verified authored documentation links and bibliography metadata.
